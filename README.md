# Brand Consistency QA (semantic-only) scaffold

This is the modularized scaffold version of your single-cell v5.1.22 semantic-only pipeline.

## Run
python scripts/run_single.py

## Notes
- No stdout prints by design.
- Outputs saved to the paths configured inside scripts/run_single.py
